class Gateway{
  late Uri gatewayUrl;
  late String merchantSecret;
  
  // Map<String, String> directRequest({required final Map<String, String> request,required final Map<String, String> options}){
  //   Uri directUrl;
  //   if(request.containsKey("directUrl")){
  //     directUrl = Uri.parse(request['directUrl']!);
  //
  //   }else{
  //     directUrl = this.gatewayUrl;
  //   }
  // }
}